[package]
name = "crypto-validation"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# The reference: the published crate, whose sources are commit 4f2b791 (tag 0.14.0).
bc-crypto = "=0.14.0"
bc-rand = "=0.5.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
rand_core = "0.6"
rand = "0.9"
